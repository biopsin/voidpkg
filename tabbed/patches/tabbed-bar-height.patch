diff --git a/tabbed.c b/tabbed.c
index eafe28a..aaea165 100644
--- a/tabbed.c
+++ b/tabbed.c
@@ -984,7 +984,7 @@
 	screen = DefaultScreen(dpy);
 	root = RootWindow(dpy, screen);
 	initfont(font);
-	bh = dc.h = dc.font.height + 2;
+	bh = dc.h = barHeight;
 
 	/* init atoms */
 	wmatom[WMDelete] = XInternAtom(dpy, "WM_DELETE_WINDOW", False);
